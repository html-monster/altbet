import {
	ON_TRADER_ON,
	ON_TAB_SWITCH
} from '../constants/ActionTypesSidebar';


export function actionTest()
{
    // return (dispatch, getState) => {
    //     dispatch({
    //         type: ON_TEST,
    //         payload: { Chart: {} }
    //     });
    // }
}
